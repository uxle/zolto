---
title: API Reference
---

# API Reference

## functionName(param)

One-sentence description.

**Parameters:**

`param` (string)
: What this parameter controls.

**Returns:** `ReturnType` — what it returns and why.

**Example:**

```javascript
const result = functionName('value');
```

**Throws:**

- `TypeError` — if `param` is not a string.
