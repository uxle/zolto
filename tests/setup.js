/**
 * Zolto Test Setup — tests/setup.js
 * Global setup run before any test suite.
 * For Phase 2, the main suite is tests/run-all.js.
 */
export const TEST_VERSION = '2.0.0';
export const TEST_PHASE   = 2;
